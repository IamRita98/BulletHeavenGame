using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class MusicManager : MonoBehaviour
{
/*    public AudioClip mainMenuMusic;
    public AudioClip testLevelMusic;
    public AudioSource aSource;

    private void OnEnable()
    {
        SceneManager.activeSceneChanged += PickMusic;
    }
    private void OnDisable()
    {
        SceneManager.activeSceneChanged -= PickMusic;
    }

    void PickMusic(Scene oldScene, Scene newScene)
    {
        switch (newScene.name)
        {
            //Todo: Add a fade out and in to music
            case ("TestLevel"):
                ChangeMusic(testLevelMusic);
                break;
            case ("MainMenu"):
                ChangeMusic(mainMenuMusic);
                break;
        }
    }

    void ChangeMusic(AudioClip audioToPlay)
    {
        aSource.clip = audioToPlay;
        aSource.Play();
    }*/
}
